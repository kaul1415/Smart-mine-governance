const prisma = require('../config/db');const {  createComplianceSchema,  updateComplianceSchema,} = require('../validators/compliance.validator');const { logAudit } = require('../utils/auditLogger');const { ZodError } = require('zod');/** * List statutory compliances with optional filtering by mine, category, or status * GET /api/compliances */const getCompliances = async (req, res) => {  try {    const { mineId, category, status, page = '1', limit = '20' } = req.query;    const where = {};    if (mineId) where.mineId = mineId;    if (category) where.category = category;    if (status) where.status = status;    const pageNum = parseInt(page, 10);    const limitNum = parseInt(limit, 10);    const skip = (pageNum - 1) * limitNum;    const [total, compliances] = await Promise.all([      prisma.statutoryCompliance.count({ where }),      prisma.statutoryCompliance.findMany({        where,        skip,        take: limitNum,        orderBy: { updatedAt: 'desc' },        include: {          mine: {            select: { id: true, name: true, code: true, subsidiary: true, state: true },          },        },      }),    ]);    return res.status(200).json({      success: true,      pagination: {        total,        page: pageNum,        limit: limitNum,        totalPages: Math.ceil(total / limitNum),      },      data: compliances,    });  } catch (error) {    console.error('getCompliances error:', error);    return res.status(500).json({ success: false, message: 'Failed to fetch compliance records' });  }};/** * Get compliance overview summary across categories (Safety, Environment, Production, Labour) * GET /api/compliances/summary */const getComplianceSummary = async (req, res) => {  try {    const { mineId } = req.query;    const where = mineId ? { mineId } : {};    const [total, compliant, nonCompliant, underReview, expired] = await Promise.all([      prisma.statutoryCompliance.count({ where }),      prisma.statutoryCompliance.count({ where: { ...where, status: 'COMPLIANT' } }),      prisma.statutoryCompliance.count({ where: { ...where, status: 'NON_COMPLIANT' } }),      prisma.statutoryCompliance.count({ where: { ...where, status: 'UNDER_REVIEW' } }),      prisma.statutoryCompliance.count({ where: { ...where, status: 'EXPIRED' } }),    ]);    const complianceRate = total > 0 ? ((compliant / total) * 100).toFixed(1) : '100.0';    return res.status(200).json({      success: true,      data: {        total,        compliant,        nonCompliant,        underReview,        expired,        complianceRatePercentage: parseFloat(complianceRate),      },    });  } catch (error) {    console.error('getComplianceSummary error:', error);    return res.status(500).json({ success: false, message: 'Failed to fetch compliance summary' });  }};/** * Create a new statutory compliance requirement * POST /api/compliances */const createCompliance = async (req, res) => {  try {    const validatedData = createComplianceSchema.parse(req.body);    const mine = await prisma.mine.findUnique({ where: { id: validatedData.mineId } });    if (!mine) {      return res.status(404).json({ success: false, message: 'Referenced Mine not found' });    }    const compliance = await prisma.statutoryCompliance.create({      data: {        ...validatedData,        validUntil: validatedData.validUntil ? new Date(validatedData.validUntil) : null,      },    });    await logAudit({      userId: req.user?.userId,      action: 'COMPLIANCE_CREATED',      entity: 'StatutoryCompliance',      entityId: compliance.id,      metadata: { mineId: compliance.mineId, title: compliance.title, category: compliance.category },    });    return res.status(201).json({      success: true,      message: 'Compliance record created successfully',      data: compliance,    });  } catch (error) {    if (error instanceof ZodError) {      return res.status(400).json({        success: false,        message: 'Validation failed',        errors: error.errors.map((e) => ({ field: e.path.join('.'), message: e.message })),      });    }    console.error('createCompliance error:', error);    return res.status(500).json({ success: false, message: 'Failed to create compliance record' });  }};/** * Update a statutory compliance record * PUT /api/compliances/:id */const updateCompliance = async (req, res) => {  try {    const { id } = req.params;    const validatedData = updateComplianceSchema.parse(req.body);    const existing = await prisma.statutoryCompliance.findUnique({ where: { id } });    if (!existing) {      return res.status(404).json({ success: false, message: 'Compliance record not found' });    }    const updated = await prisma.statutoryCompliance.update({      where: { id },      data: {        ...validatedData,        ...(validatedData.validUntil && { validUntil: new Date(validatedData.validUntil) }),      },    });    await logAudit({      userId: req.user?.userId,      action: 'COMPLIANCE_UPDATED',      entity: 'StatutoryCompliance',      entityId: id,      metadata: { changes: validatedData },    });    return res.status(200).json({      success: true,      message: 'Compliance record updated successfully',      data: updated,    });  } catch (error) {    if (error instanceof ZodError) {      return res.status(400).json({        success: false,        message: 'Validation failed',        errors: error.errors.map((e) => ({ field: e.path.join('.'), message: e.message })),      });    }    console.error('updateCompliance error:', error);    return res.status(500).json({ success: false, message: 'Failed to update compliance record' });  }};/** * Delete a statutory compliance record * DELETE /api/compliances/:id */const deleteCompliance = async (req, res) => {  try {    const { id } = req.params;    const existing = await prisma.statutoryCompliance.findUnique({ where: { id } });    if (!existing) {      return res.status(404).json({ success: false, message: 'Compliance record not found' });    }    await prisma.statutoryCompliance.delete({ where: { id } });    await logAudit({      userId: req.user?.userId,      action: 'COMPLIANCE_DELETED',      entity: 'StatutoryCompliance',      entityId: id,      metadata: { title: existing.title, mineId: existing.mineId },    });    return res.status(200).json({      success: true,      message: 'Compliance record deleted successfully',    });  } catch (error) {    console.error('deleteCompliance error:', error);    return res.status(500).json({ success: false, message: 'Failed to delete compliance record' });  }};module.exports = {  getCompliances,  getComplianceSummary,  createCompliance,  updateCompliance,  deleteCompliance,};